# Changelog

## v1.1.0

- Auto-update system for pip packages and bot code via `/manage`.
- Moved `db.py` and `localization.py` into `core/`.
- Cookie path moved to `db/cookies.txt`.
- SpotAPI moved to PyPI in `requirements.txt`.

## v1.0.0

Initial release.
